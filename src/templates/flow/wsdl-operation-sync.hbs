{{! TODO: Should type the error based on the soap faults... }}
{{operationName}}: (
    input: {{inputType}} | {| _xml: string |}, 
    (
        err?: any, 
        result: {{outputType}}, 
        rawResponse: string, 
        soapHeader: Object, 
        rawRequest: string
    ) => void
) => void,
