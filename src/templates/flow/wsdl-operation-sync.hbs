{{! TODO: Should type the error based on the soap faults... }}
{{operation.$.name.value}}: ({{#if input}}input: {{localPart input.$.message.value}} | {| _xml: string |}{{/if}}, (err?: any, result: {{#if output}}{{localPart output.$.message.value}}{{else}}null{{/if}}, rawResponse: string, soapHeader: Object, rawRequest: string) => void) => void,
